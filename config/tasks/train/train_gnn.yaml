epochs: 10

report_interval: 100
val_interval: 100
num_val_batches: 4

checkpointing: False

data_loaders:
  train:
    split_key: train_idxs
    batch_size: 4
    num_workers: 0
  validation:
    split_key: val_idxs
    batch_size: 4
    num_workers: 0
