# @package _group_
epochs: 1

report_interval: 50
val_interval: 100
num_val_batches: 32

train_data:
  split_key: train_idxs
  batch_size: 512
  transforms:
    - horizontal_flip
    - vertical_flip
validation_data:
  split_key: val_idxs
  batch_size: 512